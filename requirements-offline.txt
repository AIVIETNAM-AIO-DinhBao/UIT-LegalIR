# Keep this list intentionally small. Kaggle's GPU image supplies torch,
# transformers, NumPy, SciPy, scikit-learn, PyYAML, and their dependencies as a
# tested stack. Replacing those packages in a live notebook can mix Python files
# and binary extensions from different versions and can replace CUDA-enabled torch.
faiss-cpu>=1.8.0
sentence-transformers>=5.4.1,<6
# Kaggle's Transformers 5.17 requires this range, but its base image can ship
# tokenizers 0.22.x. Keep the compatible tokenizer wheel in the offline bundle.
tokenizers>=0.23.1,<0.24
